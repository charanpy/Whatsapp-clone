import { HomeContainer } from "./Home.style";
import ChatList from "../../components/ChatList/ChatList";

const Home = () => {
  return (
    <HomeContainer>
      <ChatList />
    </HomeContainer>
  );
};

export default Home;
